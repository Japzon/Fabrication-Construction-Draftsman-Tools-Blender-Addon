# Fabrication & Construction Draftsman Tools (Automated) - AI Update Plan

## Objective
Fix persistent UI errors in the Architectural Presets panel and enable Edit Mode vertex-to-vertex measuring in the Dimensions & Measuring toolkit.

## Tasks
1. **Fix Architectural Panel TypeError**:
   - Re-check the `draw` method signature and ensure no `self` references exist in static methods.
   - Standardize `ui_dimensions.py` to use `ui_common.draw_panel_header` to prevent manual drawing errors.
   - Add `@classmethod poll` to all drawing helpers for consistency with the master UI loop.
   
2. **Fix Dimensions Edit Mode Measuring**:
   - Update `URDF_OT_AddDimension` in `operators.py` to correctly handle vertex selection in Edit Mode.
   - Ensure `bm.verts.ensure_lookup_table()` is called before accessing vertex indices.
   - Use `bm.select_history` for ordering, with a robust fallback to the selection list if history is empty.

3. **UI Robustness**:
   - Standardize all icons and labels to use the "Literal" naming convention.
   - Ensure `draw_panel_header` is called with exactly 5 arguments in all modules.

## Procedural Safety
- **Layout Consistency**: Always use the first return value of `draw_panel_header` (the `box`) as the base for panel content.
- **BMesh Safety**: Always call `ensure_lookup_table()` after `from_edit_mesh`.
